import { LoadingOutlined } from "@ant-design/icons";
const Loading = props => {
    return (
        <div className="loading-body border_radius">
            <LoadingOutlined style={{fontSize:'15px'}} />
        </div>
        
    )
}

export default Loading;